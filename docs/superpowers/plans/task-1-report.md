I have completed Task 1: Create Sidebar Component according to the task brief.

I created the `sentinel/ui/quest_ui/sidebar.lua` file containing the `Sidebar.draw` function, matching the requested code precisely. It handles rendering the sidebar background, settings header, and the `quest_skip_elites` checkbox state using the provided `menu` table. 

After verifying the implementation is correct, I've successfully committed the changes to the repository with the requested message (`feat: add quest ui sidebar component`).

Please let me know if you are ready to proceed with the next task!
