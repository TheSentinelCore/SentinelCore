local Settings = {
    enabled = false,
    category_id = 0,
    filter = 0,
    preferred_filters = 0,
    role = "damage",
    auto_accept_invite = false,
    search_interval_ms = 15000,
}

return Settings
