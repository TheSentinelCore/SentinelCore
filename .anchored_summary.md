## Objective
- Build the Quest Authoring IDE (Tickets 004–016), wire it as the sole quest UI entry point, implement gaps #1–#5, fix `query_client.lua` syntax errors, and resolve the live-sync problem (untracked new files omitted from manual copy).

## Important Details
- **Sylvannas API only**; `luajit` at `/usr/bin/luajit`. Local `package.path` needs BOTH `…/SentinelCore/sentinel/?.lua` and `…/SentinelCore/?.lua` (for `sentinel.modules.quest.*` / `sentinel.lib.*`).
- `JSON` at `sentinel/lib/JSON.lua`; `SourceAST:parse_project(dir)` → `{ok, ast, errors}`; `ProfileExecutor.new(blackboard, engine, nav_adapter, eventBus)`; `app`: `get_blackboard`/`get_event_bus`/`get_module`/`_nav_adapter`.
- Deleted old UI: `ui/quest_ui`, `ui/tabs`, `ui/profile_editor`. UI entry: `ui/window.lua` → `runtime/app.lua:on_render_window` → `main.lua`.
- **SYNC ROOT CAUSE (RESOLVED IN REPO, NEEDS LIVE COPY)**: all new files (`sentinel/ui/quest_authoring/*`, ~20 `sentinel/modules/quest/*.lua`) were untracked; a selective/manual copy omitted them, causing cascading "not found". FIX: committed everything (commit `86e00f8`, 100 files). User does **manual folder copy** (not git pull) → must copy the ENTIRE `sentinel` folder (full overwrite) so new dirs/files arrive. After copy, verify `scripts\sentinel\ui\quest_authoring\init.lua` exists, then `/reload`.
- `query_client.lua` had **four** stray `}` (lines ~230/293/359/409) — all fixed in repo; full-repo parse sweep clean.
- Repo remote `origin` = github.com/TheSentinelCore/SentinelCore.git (master). Not pushed (user copies manually).

## Work State
### Completed
- Tickets 004–007 (compiler passes + `source_ast`), 016 (`profile_executor` hotSwap), 008–015 (9 panes + shared base), `init.lua`, UI wiring.
- Gaps #1–#5 implemented; #4 left as documented approximate.
- `query_client.lua` stray-`}` fixed; full-repo parse sweep clean.
- All new files committed (`86e00f8`). Smoke test `/tmp/smoke_qa.lua` passes (465 draw calls).
### Active / Not Done
- **Live copy**: user must full-copy `sentinel/` to live `scripts\sentinel\` so committed new files arrive. Pending in-game `/reload` success.
- Gap #6 (in-game runtime verification) pending live copy + session.

## Next Move
1. User full-copies `sentinel/` → live `scripts\sentinel\` (overwrite); confirm `ui\quest_authoring\init.lua` present.
2. In-game `/reload`; confirm no "not found" init errors; open "Sentinel Quest Authoring".
3. `ide:loadProjectPath("<path>")` or use sample; click **Run**; verify live.

## Relevant Files
- `sentinel/ui/quest_authoring/*` — IDE (NEW, committed `86e00f8`)
- `sentinel/modules/quest/{compiler_pass1..4,profile_executor,source_ast,statechart_executor,core_actions,events,profile_actions,profile_compiler,profile_loader,quest_data,quest_registry,routing_policy_loader,source_schema,source_validator,step_executors}.lua` — NEW, committed
- `sentinel/modules/quest/query_client.lua` — stray-`}` fixed
- `sentinel/ui/window.lua`, `sentinel/runtime/app.lua`, `sentinel/main.lua` — tracked+modified, committed
