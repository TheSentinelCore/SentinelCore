return {
    map_ids = require("shared/map_ids"),
    queue_priorities = require("shared/queue_priorities"),
    blackboard_keys = require("shared/blackboard_keys"),
    capability_registry = require("shared/capability_registry"),
}
